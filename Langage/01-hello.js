// // @ts-check
/**
 * Additionne 2 nombres
 * @param {number} a Le 1er nb
 * @param {number} b Le 2e nb
 * @returns {number} La somme
 */
const sum = (a, b) => a + b;

for (let i = 0; i < 3; i++) {
  console.log(sum(i, i));
}
